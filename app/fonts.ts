import { DM_Sans, Forum } from 'next/font/google';

export const dmSans = DM_Sans({
    subsets: ['latin-ext'], 
    weight: ['400', '700'] 
});

export const forum = Forum({
    subsets: ['latin-ext'],
    weight: ['400'] 
});


